"""Diagnostic catalog: the authoritative set of compiler diagnostics.

The catalog is parsed from Clang's TableGen `.td` definition files (vendored
as the pinned LLVM submodule) and provides the coverage denominator.
"""
